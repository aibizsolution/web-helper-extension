# 웹 도우미

웹페이지 번역, 텍스트 번역, 검색 보조, 페이지 진단, 브라우저 도구, 반복 체크리스트를 한 사이드패널에서 처리하는 Chrome 확장 프로그램입니다.

개발 문서: [AGENTS.md](./AGENTS.md)  
변경 기록: [CHANGELOG.md](./CHANGELOG.md)

## 현재 버전
- 버전: `2.4.0`
- 마지막 정리: `2026-03-13`

## 주요 기능
- `번역`
  - 메인 탭 하나 안에서 `사이트 / 텍스트 / 기록` 서브탭으로 번역 작업을 묶어 제공합니다.
- `사이트 번역`
  - API Key 없이 빠르게 읽는 구글 번역과, 선택한 provider/model을 쓰는 AI 정밀 번역을 제공합니다.
- `텍스트 번역`
  - 짧은 텍스트를 직접 입력해 번역하고, 같은 서브탭에서 기록도 다시 볼 수 있습니다.
- `선택 텍스트 번역`
  - 드래그 액션바와 우클릭 메뉴에서 번역/복사/설명하기/검색을 빠르게 실행할 수 있습니다.
- `스마트 검색`
  - 검색 의도 기반 추천 검색문으로 여러 검색 엔진을 열 수 있습니다.
- `페이지 진단`
  - 검색 노출과 AI 노출 요소를 함께 점검합니다.
- `도구`
  - 브라우저 캐시/사이트 데이터 정리와 기간별 방문 TOP 확인을 제공합니다.
- `오류 센터`
  - 최근 오류/경고 로그 확인과 요약 복사, 로그 정리를 제공합니다.
- `반복관리`
  - 카테고리형 반복 체크리스트를 관리할 수 있습니다.

## UI 구조 원칙
- `sidepanel.html`은 패널 골격과 접근성용 마크업 위주로 유지합니다.
- 스타일은 `styles/` 아래 `tokens`, `layout`, `components`, `feature-panels`로 분리합니다.
- 메인 탭 lifecycle은 `modules/tab-registry.js`와 `modules/panel-tab-modules.js`를 통해 연결합니다.
- 기준은 `모바일 대응`이 아니라 `데스크톱 Chrome 사이드패널 폭 최적화`입니다.

## 지원 AI provider
- `OpenRouter`
  - `google/gemini-3.1-flash-lite-preview`
  - `openai/gpt-5-nano`
- `OpenAI`
  - `gpt-5-nano`
- `Gemini`
  - `gemini-3.1-flash-lite-preview`

구글 번역은 별도 API Key 없이 동작합니다.  
AI 정밀 번역은 설정에서 provider API Key를 입력해야 사용할 수 있습니다.

## 설치 방법
1. 이 저장소를 내려받거나 clone 합니다.
2. 개발 환경을 맞추려면 루트에서 `npm install`을 실행합니다.
3. Chrome에서 `chrome://extensions/`를 엽니다.
4. 우측 상단 `개발자 모드`를 켭니다.
5. `압축해제된 확장 프로그램 로드`를 눌러 이 폴더를 선택합니다.
6. 툴바 또는 단축키 `Ctrl+Shift+Y`로 사이드패널을 엽니다.

## 개발 시작
- 이 확장은 실행 자체에는 별도 빌드가 필요 없습니다.
- 새로 받으면 먼저 `npm install`로 개발 도구를 맞추고, 그 다음 Chrome에 압축해제된 확장으로 로드하면 됩니다.
- 기본 검증은 `npm run check`로 돌립니다.
- provider smoke test가 필요할 때만 `.env.providers.example`을 참고해 `.env.providers.local`을 준비한 뒤 `npm run test:providers`를 사용합니다.

## 기본 사용 흐름
1. `설정` 탭에서 필요한 provider API Key를 입력합니다.
2. `번역` 탭의 `사이트` 서브탭에서 provider와 모델을 고릅니다.
3. `구글 번역` 또는 `AI 정밀 번역`을 실행합니다.
4. 텍스트를 직접 번역하려면 같은 탭의 `텍스트` 서브탭을 사용합니다.
5. 필요하면 같은 탭의 `기록` 서브탭에서 최근 결과를 다시 엽니다.
6. 페이지에서 텍스트를 드래그하면 액션바로 번역/복사/설명하기/검색을 빠르게 실행할 수 있습니다.

## 업데이트 방법
1. 최신 코드를 내려받아 기존 폴더에 덮어씁니다.
2. `chrome://extensions/`에서 `웹 도우미`를 다시 로드합니다.
3. 이미 열려 있던 웹페이지 탭도 새로고침합니다.
   - content script가 업데이트되려면 열린 탭 새로고침이 필요할 수 있습니다.

## 릴리스 체크
- `manifest.json` 버전 확인
- `meta.js` 날짜 확인
- `CHANGELOG.md`와 `README.md` 최근 변경 확인
- `npm run check`
- 필요 시 `npm run test:providers`
- 확장 다시 로드 후 사용 탭 새로고침

## 최근 변경

### 2026-03-13
- 우측 탭바를 `번역 / 페이지 진단 / 검색 / 도구 / 반복관리 / 설정 / 오류 센터 / 최신버전` 구조로 다시 정리
- `사이트`와 `텍스트`를 메인 탭에서 분리하지 않고, `번역` 탭 안의 `사이트 / 텍스트 / 기록` 서브탭으로 통합
- `npm install`, `npm run check`, `npm run test:providers` 기준으로 개발 온보딩을 단순화
- 변경 기록 관리를 위해 `CHANGELOG.md`를 추가

### v2.4.0 · 2026-03-12
- 제품명을 `웹 도우미`로 정리
- 번역 탭을 `현재 페이지 / 번역 기록` 구조로 재편
- 구글 번역과 AI 정밀 번역 UX 분리
- provider/model 선택을 멀티 provider 구조로 정비
- 선택 텍스트 번역 UX와 복사 흐름 개선
- 사이드패널 단축키 토글 추가
- 개발 문서를 `AGENTS.md` 기준으로 재정비
